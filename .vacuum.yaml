# vacuum ruleset — descriptions-only CI gate (D-04)
#
# Examples (D-05) are AI-generated bonus content, not CI-enforced. Tightening
# to require examples= is a post-launch decision tracked in the docs backlog.

extends:
  - - spectral:oas
    - off
rules:
  operation-description: error
  oas3-parameter-description: error
  component-description: error
