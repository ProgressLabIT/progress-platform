{
  "name": "progress-test",
  "version": "0.0.1",
  "description": "Automated testing project for progress platform",
  "main": "index.js",
  "scripts": {
    "open-cypress": "npx cypress open",
    "clean": "rimraf ./mochawesome-report",
    "test": "npm run clean && mocha ./test-reqres-api --reporter spec --reporter mochawesome --timeout 5000",
    "test-example": "npm run clean && mocha ./test-reqres-api/examples --reporter spec --reporter mochawesome --timeout 5000",
    "test-fakerest": "npm run clean && mocha ./test-fakerest-api --reporter spec --reporter mochawesome --timeout 5000",
    "test-fakerest-example": "npm run clean && mocha ./test-fakerest-api/examples --reporter spec --reporter mochawesome --timeout 5000",
    "test-conf": "mocha --config=.mocharc.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.10.0",
    "@faker-js/faker": "^8.0.2",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "rimraf": "^5.0.1",
    "supertest": "^7.0.0"
  }
}
