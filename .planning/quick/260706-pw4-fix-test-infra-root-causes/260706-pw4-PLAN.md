---
phase: quick-260706-pw4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testing/pytest/pyproject.toml
  - testing/pytest/uv.lock
  - testing/pytest/conftest_helpers/schema.py
  - testing/pytest/tests/production/test_job_lifecycle.py
  - testing/pytest/tests/production/test_batch_state.py
  - testing/pytest/tests/api/test_schemathesis.py
autonomous: true
requirements: [QUICK-260706-PW4]

must_haves:
  truths:
    - "Test venv resolves bcrypt 4.3.x + passlib 1.7.2, matching backend runtime pins — auth endpoints no longer 500 from the passlib/bcrypt-5 self-test break"
    - "Bare `uv run pytest` deselects the 10 integration-marked sparkplug tests by default; `-m integration` on the CLI still selects them"
    - "ErrorLog collection exists in PROGRESS_TEST during test runs, so 5xx error-logging inserts no longer fail with 'collection or view not found'"
    - "Production test payload helpers read the product key via g['product']['product_key'], matching create_production_graph's return shape"
    - "Schemathesis fuzz is capped at 10 hypothesis examples per operation (~1,650 cases instead of ~16,500)"
  artifacts:
    - path: "testing/pytest/pyproject.toml"
      provides: "bcrypt==4.3.*, passlib==1.7.2, addopts = \"-m 'not integration'\""
      contains: "addopts"
    - path: "testing/pytest/conftest_helpers/schema.py"
      provides: "ErrorLog document collection in test schema init"
      contains: "Collection(name='ErrorLog')"
    - path: "testing/pytest/tests/production/test_job_lifecycle.py"
      provides: "_event_payload using product_key"
      contains: "g[\"product\"][\"product_key\"]"
    - path: "testing/pytest/tests/production/test_batch_state.py"
      provides: "_event_payload using product_key"
      contains: "g[\"product\"][\"product_key\"]"
    - path: "testing/pytest/tests/api/test_schemathesis.py"
      provides: "hypothesis settings cap on fuzz test"
      contains: "max_examples=10"
  key_links:
    - from: "testing/pytest/conftest_helpers/schema.py"
      to: "testing/pytest/conftest.py db fixture"
      via: "initialize_schema(sys_db) call at conftest.py:103-104"
      pattern: "Collection\\(name='ErrorLog'\\)"
    - from: "testing/pytest/tests/production/*_event_payload"
      to: "create_production_graph return dict (conftest.py:1181-1194)"
      via: "g['product'] is product_data keyed by product_key"
      pattern: "product_key"
---

<objective>
Fix 5 test-infra root causes identified in the 2026-07-06 failing-tests report: bcrypt/passlib version-combo breaking all auth tests, integration-marked tests failing as noise when the docker stack is down, missing ErrorLog collection masking real errors, product-key fixture contract drift in two production test files, and an unbounded schemathesis fuzz (~16,500 cases, 1+ hour).

Purpose: restore a green, fast default test run so real regressions are visible again.
Output: 3-4 atomic commits on DEV touching only test-infra files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@testing/pytest/pyproject.toml
@testing/pytest/conftest_helpers/schema.py
@testing/pytest/conftest.py
@testing/pytest/tests/production/test_job_lifecycle.py
@testing/pytest/tests/production/test_batch_state.py
@testing/pytest/tests/api/test_schemathesis.py
@backend/api/requirements.txt

<interfaces>
<!-- Verified from codebase 2026-07-06. Executor should use these directly — no exploration needed. -->

IMPORTANT path correction vs. the diagnosis report: `testing/pytest/tests/conftest.py` is a
4-line stub. The real fixtures live in `testing/pytest/conftest.py` (repo-relative), and the
schema/collection bootstrap lives in `testing/pytest/conftest_helpers/schema.py`.

From backend/api/requirements.txt (source of truth for pins):
  line 1: bcrypt==4.3.*
  line 9: passlib==1.7.2

From testing/pytest/pyproject.toml (current, to be changed):
  line 13: "passlib>=1.7",
  line 14: "bcrypt>=4.3",
  lines 39-45: [tool.pytest.ini_options] has asyncio_mode, pythonpath, testpaths, markers —
  NO addopts key yet. The `integration` marker is already declared at line 44.

From testing/pytest/conftest_helpers/schema.py:
  COLLECTIONS: list[Collection] (lines 20-252) — each entry is
  Collection(name=..., indexes=[DBIndex(...)], default_records=[...]), all fields optional
  except name. Entries are roughly alphabetical.
  initialize_schema(sys_db) (line 255) iterates COLLECTIONS; a name starting with a
  LOWERCASE letter is created as an edge collection — 'ErrorLog' starts uppercase, so it
  becomes a document collection automatically.
  NOTE: DBIndex has no expireAfter field, so the production TTL index cannot be expressed
  here — that is fine, TTL is explicitly optional for tests. Add the collection with no
  indexes and no default_records.

From testing/pytest/conftest.py:
  db fixture (line 86) calls initialize_schema(sys_db) at lines 103-104 — adding ErrorLog
  to COLLECTIONS is sufficient; no conftest.py change needed.
  create_production_graph inner _create returns (lines 1181-1194):
    {"user": user, "product": product_data, "work_order": wo, "job": job, ...}
  where product_data carries the key under "product_key" (see usages at lines 1136, 1146,
  1155: product_data["product_key"]). It is NOT a raw Arango document — no "_key" field.

From testing/pytest/tests/api/test_schemathesis.py:
  line 105-106:
    @schema.parametrize()
    def test_api_fuzzing(case):
  Hypothesis settings go BETWEEN parametrize and the function:
    @schema.parametrize()
    @settings(max_examples=10, deadline=None)
    def test_api_fuzzing(case):
  with `from hypothesis import settings` added to the imports (hypothesis is already a
  transitive dep of schemathesis; do not add it to pyproject).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin bcrypt/passlib to backend versions and default-exclude integration tests</name>
  <files>testing/pytest/pyproject.toml, testing/pytest/uv.lock</files>
  <action>
    Two edits to testing/pytest/pyproject.toml (Fixes 1 and 2 from the report — same file, one commit):

    1. Dependency pins: change `"passlib>=1.7"` to `"passlib==1.7.2"` and `"bcrypt>=4.3"` to
       `"bcrypt==4.3.*"` in [project].dependencies, matching backend/api/requirements.txt exactly.
       Rationale: the test venv resolved bcrypt 5.0.0 + passlib 1.7.4, whose bcrypt-backend
       self-test raises `ValueError: password cannot be longer than 72 bytes`, turning every
       auth call into a 500.

    2. Default marker exclusion: add `addopts = "-m 'not integration'"` to
       [tool.pytest.ini_options] (there is no existing addopts key). The `integration` marker
       is already declared at line 44; 10 sparkplug tests carry it and fail as pure noise when
       the deployed stack is down. A CLI `-m integration` still overrides (last -m wins).

    Then run `cd testing/pytest && uv sync` to re-resolve the lockfile (uv.lock will change —
    stage it with pyproject.toml).

    Do NOT re-investigate the diagnosis; it is settled.

    Commit (stage ONLY testing/pytest/pyproject.toml and testing/pytest/uv.lock — the repo has
    unrelated dirty files; never `git add -A`):
    `fix(tests): pin bcrypt/passlib to backend versions, default-exclude integration marker`
  </action>
  <verify>
    <automated>cd testing/pytest && uv sync && uv run python -c "import bcrypt, passlib; print(bcrypt.__version__, passlib.__version__)" && export DOCKER_HOST=unix:///Users/$USER/.docker/run/docker.sock && uv run pytest tests/api/test_auth.py</automated>
  </verify>
  <done>bcrypt reports 4.3.x and passlib 1.7.2; the 8 previously-failing auth tests pass; `uv run pytest --collect-only -q | tail -3` reports the 10 integration tests as deselected; commit contains only the two files.</done>
</task>

<task type="auto">
  <name>Task 2: Add ErrorLog to test schema init and fix product-key fixture drift</name>
  <files>testing/pytest/conftest_helpers/schema.py, testing/pytest/tests/production/test_job_lifecycle.py, testing/pytest/tests/production/test_batch_state.py</files>
  <action>
    Fix 3 — ErrorLog collection: the `client` fixture uses httpx ASGITransport, which never
    fires the app lifespan, so main.py's `error_log.ensure_collection()` never runs and
    5xx error-logging inserts fail with `DocumentInsertError ... collection or view not found:
    ErrorLog`, masking real errors. Add `Collection(name='ErrorLog'),` to the COLLECTIONS list
    in testing/pytest/conftest_helpers/schema.py, following the existing pattern (insert in
    alphabetical position, near `Collection(name='Event', ...)`). No indexes, no
    default_records — the DBIndex model has no expireAfter field and the TTL index is
    explicitly optional for tests. 'ErrorLog' starts uppercase so initialize_schema creates it
    as a document collection (lowercase-first names become edge collections).

    Fix 4 — fixture contract drift: in BOTH testing/pytest/tests/production/test_job_lifecycle.py
    and test_batch_state.py, the `_event_payload()` helper reads `g["product"]["_key"]`, but
    create_production_graph (testing/pytest/conftest.py:1183) returns `"product": product_data`,
    a dict keyed `product_key` (verified — no `_key` field). Change the payload line to
    `"product_key": g["product"]["product_key"],` in both helpers. No other lines in these
    helpers change (`g["user"]`, `g["job"]`, `g["work_order"]`, `g["target_phase"]` are raw
    documents and correctly use `_key`).

    For the collaboration verify below: if tests/collaboration/test_issues.py now fails with a
    REAL underlying error surfaced by working error logging, REPORT what surfaces in the
    summary — do NOT chase or fix the underlying bug in this task.

    Commits (two logical fixes — two atomic commits, staging only the listed files each time):
    1. `fix(tests): create ErrorLog collection in test schema init` — schema.py only
    2. `fix(tests): read product_key from production graph fixture in payload helpers` — the two test files
  </action>
  <verify>
    <automated>cd testing/pytest && export DOCKER_HOST=unix:///Users/$USER/.docker/run/docker.sock && uv run pytest tests/production/ tests/collaboration/test_issues.py</automated>
  </verify>
  <done>tests/production/ passes with the product_key fix; tests/collaboration/test_issues.py either passes or its failures now show the real underlying error instead of ErrorLog DocumentInsertError noise (outcome reported in summary); two atomic commits with correct staging.</done>
</task>

<task type="auto">
  <name>Task 3: Cap schemathesis fuzz at 10 examples per operation</name>
  <files>testing/pytest/tests/api/test_schemathesis.py</files>
  <action>
    Fix 5 — 165 API operations x hypothesis default 100 examples = ~16,500 cases (1+ hour).
    In testing/pytest/tests/api/test_schemathesis.py:

    1. Add `from hypothesis import settings` to the imports (hypothesis is already installed
       as a schemathesis dependency — do not touch pyproject).
    2. Decorate the fuzz test, settings between parametrize and the function per the
       schemathesis 4.x pattern:
       `@schema.parametrize()` then `@settings(max_examples=10, deadline=None)` then
       `def test_api_fuzzing(case):`.

    Do NOT run the full fuzz as verification — collection-only check.

    Commit (stage ONLY this file):
    `fix(tests): cap schemathesis fuzz at 10 examples per operation`
  </action>
  <verify>
    <automated>cd testing/pytest && export DOCKER_HOST=unix:///Users/$USER/.docker/run/docker.sock && uv run pytest tests/api/test_schemathesis.py --collect-only -q | tail -3</automated>
  </verify>
  <done>Collection succeeds with no import/decorator errors; the settings decorator with max_examples=10 and deadline=None is applied; single-file atomic commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Test-infra-only changes; no production code paths, endpoints, or inputs altered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q260706pw4-SC | Tampering | uv dependency re-resolution | accept | No new packages introduced. bcrypt and passlib are existing, well-established deps already in both the backend runtime and test venv; the change pins them DOWN to the exact versions the backend has run in production (backend/api/requirements.txt is the source of truth). `hypothesis` is already a transitive dep of schemathesis — not added. |
| T-q260706pw4-01 | Repudiation | ErrorLog in test schema | accept | Test-database-only collection creation mirroring the production `ensure_collection()`; improves (not weakens) error observability in tests. |
</threat_model>

<verification>
From `cd testing/pytest` with `export DOCKER_HOST=unix:///Users/$USER/.docker/run/docker.sock` in the same shell:

1. `uv run python -c "import bcrypt, passlib; print(bcrypt.__version__, passlib.__version__)"` → 4.3.x and 1.7.2
2. `uv run pytest --collect-only -q | tail -3` → reports 10 deselected (integration marker)
3. `uv run pytest tests/api/test_auth.py tests/production/` → passes
4. `uv run pytest tests/api/test_schemathesis.py --collect-only -q | tail -3` → collects cleanly
5. `git log --oneline -4` → 3-4 atomic fix(tests) commits, no co-author trailers; `git status` still shows the unrelated dirty files (backend/api/endpoints/traceability.py, webapps/, km/, untracked serial test) untouched and unstaged
</verification>

<success_criteria>
- Auth tests green under bcrypt 4.3.* + passlib 1.7.2
- Default pytest run deselects integration-marked tests; `-m integration` still opts in
- ErrorLog collection created by initialize_schema; collaboration test outcome reported (pass, or real error surfaced — not chased)
- Both production payload helpers use g["product"]["product_key"]; tests/production/ green
- Schemathesis fuzz capped at max_examples=10, deadline=None; collection verified
- Only the 6 files in files_modified staged/committed; unrelated dirty files untouched
</success_criteria>

<output>
Create `.planning/quick/260706-pw4-fix-test-infra-root-causes/260706-pw4-SUMMARY.md` when done. Include what surfaced from tests/collaboration/test_issues.py after the ErrorLog fix (pass, or the real underlying error now visible).
</output>
