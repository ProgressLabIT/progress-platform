---
phase: quick
plan: 260320-w3j
type: execute
wave: 1
depends_on: []
files_modified:
  - webapps/main/src/lib/print/plugins/linkConfig.js
  - webapps/main/src/lib/print/index.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Template designers can select position.code from the preset dropdown in the pdfme designer"
    - "Main app TemplateContext.getPresetValue('position.code') resolves to the position's code when a position is set on the context"
  artifacts:
    - path: "webapps/main/src/lib/print/plugins/linkConfig.js"
      provides: "position.code in presetOptions array"
      contains: "position.code"
    - path: "webapps/main/src/lib/print/index.js"
      provides: "position.code case in getPresetValue switch + position property on TemplateContext"
      contains: "position.code"
  key_links:
    - from: "webapps/main/src/lib/print/plugins/linkConfig.js"
      to: "webapps/main/src/lib/print/index.js"
      via: "preset name string 'position.code' used in both designer UI and runtime resolution"
      pattern: "position\\.code"
---

<objective>
Add `position.code` to the main app's preset field system so template designers can link fields to a position's code.

Purpose: Position labels designed in the main app's pdfme designer need `position.code` available as a preset option. The warehouse app already handles this in its own `buildPositionContext()`, but the main app's `TemplateContext` and the designer's preset dropdown are missing it.

Output: `position.code` appears in the designer preset dropdown and resolves at print time when a position is set on the context.
</objective>

<execution_context>
@/Users/luca/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@webapps/main/src/lib/print/plugins/linkConfig.js
@webapps/main/src/lib/print/index.js

<interfaces>
<!-- TemplateContext base class (from webapps/main/src/lib/print/index.js) -->
<!-- Already has: serial, product, workOrder, job, issue properties -->
<!-- Already has: setSelectedSerial(), setSelectedProduct(), setSelectedWorkOrder(), setSelectedIssue() -->
<!-- getPresetValue() switch handles: serial.*, product.*, job.*, work_order.*, project.*, issue.*, current_* -->
<!-- presetOptions in linkConfig.js lists all available preset keys for the designer dropdown -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add position.code to preset options and TemplateContext</name>
  <files>webapps/main/src/lib/print/plugins/linkConfig.js, webapps/main/src/lib/print/index.js</files>
  <action>
Two changes needed:

1. **linkConfig.js** — Add `'position.code'` to the `presetOptions` array. Place it after the `product.*` entries (after `'product.description'`), grouped as a position section:
```
  'product.code',
  'product.description',

  'position.code',

  'issue.id',
```

2. **index.js (main app)** — Two modifications in the `TemplateContext` class:

   a. Add a `position` property initialized to `null` alongside the existing `serial`, `product`, `workOrder` properties (around line 201):
   ```js
   position = null;
   ```

   b. Add `position = null;` to the `resetState()` method.

   c. Add a `setSelectedPosition(position)` method following the pattern of `setSelectedSerial`, `setSelectedProduct`, etc.:
   ```js
   setSelectedPosition(position) {
     this.position = position;
   }
   ```

   d. Add a `case 'position.code'` in the `getPresetValue()` switch, in a new "Position presets" section after the "Product presets" block (after `case 'product.description'`):
   ```js
   // Position presets
   case 'position.code':
     return this.position?.code;
   ```

   e. Add `'position'` to the `baseObjectMap` in `getExtraValue()` method so `position.extra.*` paths work:
   ```js
   const baseObjectMap = {
     serial: this.serial,
     job: this.job,
     work_order: this.workOrder,
     product: this.product,
     issue: this.issue,
     position: this.position,
   };
   ```
  </action>
  <verify>
    <automated>cd /Users/luca/dev/progress/progress-platform && grep -n "position.code" webapps/main/src/lib/print/plugins/linkConfig.js webapps/main/src/lib/print/index.js && grep -n "this.position" webapps/main/src/lib/print/index.js | head -10</automated>
  </verify>
  <done>
  - `position.code` appears in the presetOptions array in linkConfig.js
  - TemplateContext has a `position` property (null by default), a `setSelectedPosition()` method, and a `case 'position.code'` in getPresetValue()
  - `position` is included in getExtraValue()'s baseObjectMap
  </done>
</task>

</tasks>

<verification>
- `grep 'position.code' webapps/main/src/lib/print/plugins/linkConfig.js` returns a match
- `grep "case 'position.code'" webapps/main/src/lib/print/index.js` returns a match
- `grep 'position: this.position' webapps/main/src/lib/print/index.js` returns a match (in baseObjectMap)
</verification>

<success_criteria>
- Template designers see `position.code` in the preset field dropdown
- `getPresetValue('position.code')` returns `this.position?.code` when position is set
- No regressions in existing preset resolution
</success_criteria>

<output>
After completion, create `.planning/quick/260320-w3j-add-position-code-to-the-preset-fields/260320-w3j-SUMMARY.md`
</output>
