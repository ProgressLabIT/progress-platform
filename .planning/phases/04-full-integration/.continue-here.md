---
phase: 04-full-integration
task: post-gap-closure SSE debugging
status: in_progress
last_updated: 2026-03-20T17:31:15.112Z
---

<current_state>
Phase 04 gap closure plans (04-05, 04-06) are fully executed and verified. We are now doing a
second-round UAT of those fixes. Two SSE-related bugs were found and fixed inline during UAT:
both commits are on branch 536/pdfme5-upgrade. The user is currently validating whether
EventSource connections close promptly after receiving a print-result event.

The user's last observation: screenshot showed one print-result event received in the EventStream
tab, but the connection appeared still open. Open question: is this a DevTools display artifact
(connection was closed but DevTools still shows it) or is `source.close()` not running?
</current_state>

<completed_work>

- Phase 04 gap closure executed: 04-05 (btoa stack overflow fix) + 04-06 (warehouse SSE notifications)
- Verification passed 6/6
- UAT session restarted, test 1 (PDF print spinner) passed
- Test 2 found two issues:
  1. **SSE connection leak (FIXED — commit 81d5add2)**: `server_event_manager.py` `push_events` used
     blocking `queue.get()` with no timeout — dead connections never cleaned up. Fixed with
     `asyncio.wait_for(timeout=1.0)` + `finally` block + corrected `undergisterQueue` (was using
     `dict.remove()` which doesn't exist). User also added `yield {"comment": ""}` keep-alive on
     TimeoutError (uncommitted).
  2. **Race condition: subscribe-before-submit (FIXED — commit afcfeef9)**: `waitForPrintResult`
     opened EventSource AFTER job was submitted — fast results (connection-refused) arrived before
     the subscription was registered, event was lost, connection stayed open until timeout.
     Fixed in both apps: `waitForPrintResult` now accepts `Promise<string>` jobId, opens EventSource
     immediately, buffers events until jobId resolves. Callers pass
     `submitPrintJob(...).then(r => r.data.job_id)` to start subscription and submission in parallel.
  3. **"False success" on subsequent calls** — NOT a code bug. Print service log showed 192.168.1.57
     (real printer), not 127.0.0.1 (test printer). First failure was transient; subsequent prints
     genuinely succeeded.
</completed_work>

<remaining_work>

- Validate that after the race-condition fix, EventSource connections now close promptly after
  receiving the print-result event (requires frontend rebuild + retest)
- If connections are still open: investigate whether `data.job_id === jobId` comparison is
  working (check `r.job_id` vs `r.data.job_id` in the submit promise chain for each app)
- UAT test 2 is still formally "issue" status — needs a passing retest to mark complete
- `server_event_manager.py` has one uncommitted change (user-added keep-alive `yield {"comment": ""}`)
  plus 15 other files with uncommitted milestone branch work (see uncommitted_files below)
</remaining_work>

<decisions_made>

- `waitForPrintResult` accepts `Promise<string> | string` — backward compatible; strings are
  wrapped by `Promise.resolve()` transparently
- If submit promise rejects, `waitForPrintResult` propagates the rejection (outer try/catch handles it)
- Buffer approach (not replay): events buffered client-side before jobId known; server has no
  result persistence
- `asyncio.wait_for` timeout = 1.0s — balances disconnect detection speed vs CPU overhead
</decisions_made>

<blockers>

- None blocking. Open validation question about whether EventSource closes promptly after fix.
</blockers>

<context>
The SSE fix involved two layers: (1) server-side dead-connection cleanup and (2) client-side
race condition. Both are committed. The user added a keep-alive SSE comment to the backend
to help browsers detect disconnects faster — this is uncommitted. When resuming, first check
if the user has validated the fix (connections closing after receiving events), then commit the
keep-alive change and finalize the UAT.

The 15 other uncommitted files (gitlab-ci, backend models, print-service, deploy scripts, i18n,
warehouse settings) are the broader milestone branch changes — they were part of the original
branch work, not changed in this session.
</context>

<next_action>
Ask user: "Did the connections close properly after the rebuild? If yes, commit the keep-alive
change and mark UAT test 2 as passed. If no, check browser console for the job_id mismatch:
`sendToPrintService(...).then(r => console.log(r, r.job_id))` to verify the promise chain
extracts the right field."
</next_action>
