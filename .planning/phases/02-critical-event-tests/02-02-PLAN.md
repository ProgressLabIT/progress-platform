---
phase: 02-critical-event-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - testing/pytest/tests/batch/__init__.py
  - testing/pytest/tests/batch/test_batch_completed_api.py
autonomous: true
requirements: [BATCH-15, BATCH-16, BATCH-17, BATCH-21]

must_haves:
  truths:
    - "POST /event with BATCH_COMPLETED and step_check active returns 422"
    - "POST /event with BATCH_COMPLETED and quantity mismatch returns 422"
    - "POST /event with BATCH_COMPLETED and closed job returns 422"
    - "POST /event with BATCH_COMPLETED happy path returns 200 with job_data in detail"
  artifacts:
    - path: "testing/pytest/tests/batch/__init__.py"
      provides: "Package marker"
    - path: "testing/pytest/tests/batch/test_batch_completed_api.py"
      provides: "BatchCompletedEvent HTTP API test suite"
      contains: "class TestBatchCompletedAPI"
  key_links:
    - from: "testing/pytest/tests/batch/test_batch_completed_api.py"
      to: "/event"
      via: "client.post for HTTP layer"
      pattern: "client.post.*event"
---

<objective>
BatchCompletedEvent HTTP API test suite covering validation errors (422 responses) and the happy-path endpoint integration.

Purpose: Verify that the /event endpoint correctly maps BatchCompletedEvent validation errors to HTTP 422 responses, and that a successful batch completion returns 200 with the expected response shape. Per D-04: API tests assert on HTTP response shape, not DB state.

Output: `testing/pytest/tests/batch/test_batch_completed_api.py` with 4+ passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-critical-event-tests/02-RESEARCH.md

@testing/pytest/conftest.py
@testing/pytest/tests/factories/conftest.py

<interfaces>
From testing/pytest/conftest.py:
- `client` fixture: httpx.AsyncClient, base_url="http://test"
- `auth_headers` fixture: callable `_make_headers(scope: str)` sets dependency override
- `db` fixture: ArangoDB StandardDatabase

From testing/pytest/tests/factories/conftest.py:
- `create_production_graph(first_phase, last_phase, traceability_level, warehouse_management, auto_new_batch, step_check, num_steps, batch_qt, with_bom)` -> dict

From 02-RESEARCH.md:
- All events dispatch through `POST /event` with `event_type` as discriminator
- HTTP 422 body: `{"detail": {"error_type": "...", "message": "...", "exception": "..."}}`
- HTTP 200 body: `{"detail": event.response}` where response has `job_data`, `batch_data` keys
- BatchCompleted info dict keys: event_type, primary, user_key, active_batch_key, completed_batch_qt, work_session_key
- Precondition: Job.last_work_session_started must be set to work_session._key
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create batch test directory</name>
  <files>testing/pytest/tests/batch/__init__.py</files>
  <read_first>
    testing/pytest/tests/factories/conftest.py
  </read_first>
  <action>
Create `testing/pytest/tests/batch/__init__.py` as an empty file.
  </action>
  <verify>
    <automated>test -f /Users/luca/dev/progress/progress-platform/testing/pytest/tests/batch/__init__.py && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `testing/pytest/tests/batch/__init__.py` exists
  </acceptance_criteria>
  <done>Batch test package exists</done>
</task>

<task type="auto">
  <name>Task 2: BatchCompletedEvent HTTP API tests — validation errors and happy path</name>
  <files>testing/pytest/tests/batch/test_batch_completed_api.py</files>
  <read_first>
    backend/api/events/production/batch_completed.py
    backend/api/endpoints/traceability.py
    backend/api/models/traceability.py
    testing/pytest/conftest.py
    testing/pytest/tests/factories/conftest.py
  </read_first>
  <action>
Create `testing/pytest/tests/batch/test_batch_completed_api.py` with class `TestBatchCompletedAPI`:

All tests are `async` (pytest-asyncio auto mode). Each test calls `auth_headers("operator production")` before making requests.

**Common setup helper** (module-level or in class):
```python
def _batch_event_payload(g, **overrides):
    """Build a BATCH_COMPLETED event payload from a production graph."""
    return {
        "event_type": "BATCH_COMPLETED",
        "primary": True,
        "user_key": g["user"]["_key"],
        "active_batch_key": g["batch"]["_key"],
        "completed_batch_qt": g["batch"]["qt_total"],
        "work_session_key": g["work_session"]["_key"],
        **overrides,
    }
```

1. `test_step_check_active_returns_422` (BATCH-15): Create graph with `step_check=True`. Set `last_work_session_started`. POST `/event` with batch completed payload. Assert `response.status_code == 422`. Assert `"step check" in response.json()["detail"]["message"].lower()` or similar error message indicating step_check conflict.

2. `test_quantity_mismatch_returns_422` (BATCH-16): Create graph with `batch_qt=5`. Set `last_work_session_started`. POST `/event` with `completed_batch_qt=3` (mismatches `batch.qt_total` of 5.0). Assert `response.status_code == 422`. Assert response body contains error about quantity mismatch.

3. `test_closed_job_returns_422` (BATCH-17): Create graph. Set `last_work_session_started`. Close the job: `db.collection("Job").update({"_key": g["job"]["_key"], "status": "closed"})`. POST `/event` with correct payload. Assert `response.status_code == 422`. Assert response body contains error about closed job.

4. `test_happy_path_returns_200` (BATCH-21): Create graph with defaults (single phase, no traceability, no warehouse). Set `last_work_session_started`: `db.collection("Job").update({"_key": g["job"]["_key"], "last_work_session_started": g["work_session"]["_key"]})`. POST `/event` with correct payload. Assert `response.status_code == 200`. Assert `response.json()["detail"]` contains `"job_data"` key (per D-04: API layer asserts response shape only).

Important:
- Import path for endpoint: the `/event` endpoint is in `backend/api/endpoints/traceability.py`
- Always set `last_work_session_started` on Job before calling the endpoint
- The endpoint URL path may be prefixed — check the router prefix in traceability.py. If the router has a prefix, use that full path with `client.post()`
- Read `traceability.py` to find the exact route path and exception-to-422 mapping
  </action>
  <verify>
    <automated>cd /Users/luca/dev/progress/progress-platform/testing/pytest && uv run pytest tests/batch/test_batch_completed_api.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test_batch_completed_api.py` contains `class TestBatchCompletedAPI` with 4 test methods
    - `test_batch_completed_api.py` contains `test_step_check_active_returns_422`, `test_quantity_mismatch_returns_422`, `test_closed_job_returns_422`, `test_happy_path_returns_200`
    - `uv run pytest tests/batch/test_batch_completed_api.py -x -q` exits 0 with 4 tests passed
  </acceptance_criteria>
  <done>All 4 BatchCompleted HTTP API tests pass — validation errors return 422, happy path returns 200</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> FastAPI app | httpx.AsyncClient with ASGITransport; test-only auth tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | I (Information Disclosure) | test fixtures | accept | Ephemeral testcontainer, no PII |
| T-02-04 | T (Tampering) | auth bypass | accept | Test-only dependency override |
</threat_model>

<verification>
```bash
cd /Users/luca/dev/progress/progress-platform/testing/pytest && uv run pytest tests/batch/test_batch_completed_api.py -x -q
```
All 4 tests pass.
</verification>

<success_criteria>
- 4 tests pass in `tests/batch/test_batch_completed_api.py`
- Validation errors (step_check, quantity mismatch, closed job) correctly return 422
- Happy path returns 200 with expected response shape
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-event-tests/02-02-SUMMARY.md`
</output>
