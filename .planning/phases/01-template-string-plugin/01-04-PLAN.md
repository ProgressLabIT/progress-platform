---
phase: 01-template-string-plugin
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - webapps/main/src/components/PrintTemplateDesigner.vue
  - webapps/main/src/i18n/en.js
  - webapps/main/src/i18n/it.js
autonomous: false
requirements:
  - TMPL-01
  - TMPL-04

must_haves:
  truths:
    - "The designer's custom left sidebar contains a toolbar button for the 'template_string' field type"
    - "Clicking the toolbar button calls `addField('template_string')` adding a field with `linkType: 'template_expression'` to the template"
    - "`initTemplate()` decodes all `template_expression` fields (sets `content` to decoded expression) before passing to the pdfme Designer"
    - "`saveTemplate()` encodes all `template_expression` fields using `encodeExpression` before the API call"
    - "The canvas shows the decoded human-readable form (e.g. `{{cf::product_color}}`) while editing"
    - "Saving persists encoded form (e.g. `{{cf::abc123}}`) to the database"
  artifacts:
    - path: "webapps/main/src/components/PrintTemplateDesigner.vue"
      provides: "toolbar button + decode-on-load + encode-on-save for template_expression fields"
      contains: "template_string"
    - path: "webapps/main/src/i18n/en.js"
      provides: "English label for template string field type"
      contains: "field_type_template_string"
    - path: "webapps/main/src/i18n/it.js"
      provides: "Italian label for template string field type"
      contains: "field_type_template_string"
  key_links:
    - from: "webapps/main/src/components/PrintTemplateDesigner.vue"
      to: "webapps/main/src/lib/print/templateResolver.js"
      via: "import { encodeExpression, decodeExpression }"
      pattern: "encodeExpression|decodeExpression"
    - from: "PrintTemplateDesigner.vue initTemplate()"
      to: "decodeExpression"
      via: "called before workingTemplate.value assignment"
      pattern: "decodeExpression"
    - from: "PrintTemplateDesigner.vue saveTemplate()"
      to: "encodeExpression"
      via: "called on designer.getTemplate() result before API request"
      pattern: "encodeExpression"
---

<objective>
Wire the `template_string` plugin into the designer: add a toolbar button, decode expressions on load, and encode before save.

Purpose: Completes TMPL-01 (toolbar button) and TMPL-04 (encode/decode round-trip). After this plan, designers can add template string fields and see human-readable expressions in the property panel.
Output: Updated `PrintTemplateDesigner.vue` with toolbar button + encode/decode hooks + i18n keys.
</objective>

<execution_context>
@/Users/luca/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-template-string-plugin/01-CONTEXT.md
@.planning/phases/01-template-string-plugin/01-RESEARCH.md

@.planning/phases/01-template-string-plugin/01-02-SUMMARY.md
@.planning/phases/01-template-string-plugin/01-03-SUMMARY.md

<interfaces>
<!-- PrintTemplateDesigner.vue key hooks — from RESEARCH.md direct source reads -->

// initTemplate() — lines ~379–387
// Decode step slots in BEFORE cloneDeep assignment to workingTemplate.value:
function initTemplate() {
  if (props.editTemplate) {
    mode.value = 'edit';
    const cloned = migrateTemplateSchema(cloneDeep(props.editTemplate));
    // INSERT: decodeTemplateExpressions(cloned, customFields.value)
    workingTemplate.value = cloned;
  }
}

// saveTemplate() — lines ~389–401
// Encode step: read fresh from designer, encode, then send to API:
async function saveTemplate() {
  const templateFromDesigner = designer.getTemplate();
  // INSERT: const encoded = encodeAllExpressions(templateFromDesigner, customFields.value)
  // Use encoded (not templateFromDesigner) in the API payload
  await api.request({ ..., data: { ...workingTemplate.value, template: templateFromDesigner } });
}

// addField(type) — reads plugin.propPanel.defaultSchema to initialize new field
// Toolbar button pattern — existing q-btn items in custom-left-sidebar div:
// <q-btn flat ... @click="addField('text')" :label="$t('field_type_text')" />

// customFields ref — available as customFields.value in the component

// Pitfall 5 (from RESEARCH.md): decode must also set field.content = decodeExpression(field.templateExpression, customFields)
// Pitfall 3 (from RESEARCH.md): encode reads from designer.getTemplate() (fresh copy), never mutates workingTemplate.value
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add decode-on-load and encode-on-save to PrintTemplateDesigner.vue</name>
  <files>webapps/main/src/components/PrintTemplateDesigner.vue</files>
  <action>
    Read `PrintTemplateDesigner.vue` in full before modifying. Locate `initTemplate()` and `saveTemplate()`.

    **Step 1 — Import templateResolver functions:**
    At the top of the script block (near existing imports), add:
    ```javascript
    import { encodeExpression, decodeExpression } from '../lib/print/templateResolver.js';
    ```

    **Step 2 — Add helper: `decodeTemplateExpressions(template, customFields)`:**
    Create a local helper that iterates all pages and all schema fields, and for any field where `field.linkType === 'template_expression'`:
    - Sets `field.content = decodeExpression(field.templateExpression, customFields)`
    This sets the canvas preview to the human-readable decoded form.

    **Step 3 — Add helper: `encodeTemplateExpressions(template, customFields)`:**
    Create a local helper that deep-clones the template, iterates all pages and schema fields, and for any field where `field.linkType === 'template_expression'`:
    - Sets `field.templateExpression = encodeExpression(field.templateExpression, customFields)`
    Returns the encoded copy. NEVER mutate `workingTemplate.value` — always operate on the cloned template.

    **Step 4 — Slot decode into `initTemplate()`:**
    After `migrateTemplateSchema(cloneDeep(props.editTemplate))` and BEFORE `workingTemplate.value = cloned`, call:
    ```javascript
    decodeTemplateExpressions(cloned, customFields.value);
    ```

    **Step 5 — Slot encode into `saveTemplate()`:**
    After `const templateFromDesigner = designer.getTemplate()`, create an encoded copy:
    ```javascript
    const encodedTemplate = encodeTemplateExpressions(cloneDeep(templateFromDesigner), customFields.value);
    ```
    Use `encodedTemplate` (not `templateFromDesigner`) in the API request data for the `template` field.

    CRITICAL: Do NOT set `workingTemplate.value` to the encoded template. The working state must stay decoded.

    Verify vitest still passes (no regressions):
    ```
    cd webapps/main && npx vitest run src/lib/print/
    ```
  </action>
  <verify>
    <automated>cd /Users/luca/dev/progress/progress-platform/webapps/main && npx vitest run src/lib/print/</automated>
  </verify>
  <done>
    `PrintTemplateDesigner.vue` imports and calls `decodeExpression` in `initTemplate()` and `encodeExpression` in `saveTemplate()`. All vitest tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add toolbar button and i18n keys</name>
  <files>
    webapps/main/src/components/PrintTemplateDesigner.vue,
    webapps/main/src/i18n/en.js,
    webapps/main/src/i18n/it.js
  </files>
  <action>
    **In `PrintTemplateDesigner.vue`:**
    Locate the custom left sidebar div (search for `custom-left-sidebar` or existing `q-btn` items that call `addField`).
    Add a new `q-btn` for the template string field type, following the exact same pattern as the existing field type buttons:
    ```html
    <q-btn
      flat
      no-caps
      align="left"
      size="sm"
      icon="text_fields"
      @click="addField('template_string')"
      :label="$t('field_type_template_string')"
    />
    ```
    Use icon `text_fields` (Material Icons) — Claude's discretion per CONTEXT.md.
    Place it adjacent to the existing `text` field type button (logically grouped with text fields).

    **In `webapps/main/src/i18n/en.js`:**
    Find the block containing other `field_type_*` keys (e.g. `field_type_text`, `field_type_image`).
    Add: `field_type_template_string: 'Template String'`

    **In `webapps/main/src/i18n/it.js`:**
    Add: `field_type_template_string: 'Stringa Template'`
  </action>
  <verify>
    <automated>cd /Users/luca/dev/progress/progress-platform/webapps/main && npx vitest run src/lib/print/</automated>
  </verify>
  <done>
    `PrintTemplateDesigner.vue` contains a `q-btn` calling `addField('template_string')`. Both `en.js` and `it.js` contain the `field_type_template_string` key.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify designer toolbar button and encode/decode round-trip</name>
  <action>Human verification of automated work from Tasks 1 and 2.</action>
  <what-built>
    - Toolbar button in designer adds template_string field
    - initTemplate() decodes templateExpression fields on load (canvas shows slug form)
    - saveTemplate() encodes templateExpression fields before API call (DB stores key form)
  </what-built>
  <how-to-verify>
    1. Open the print template designer (navigate to a template edit page)
    2. Verify the "Template String" toolbar button appears in the left sidebar
    3. Click the button — a new field should appear in the template with `linkType: template_expression`
    4. In the property panel, find the "Expression" textarea. Type: `Product: {{product.code}} - Qty: {{serial.qt}} pcs`
    5. Observe the canvas preview — it should show the expression as typed (preset tokens show as-is)
    6. If you have a custom field named "Product Color", type `{{cf::product_color}}` — the canvas preview should show the slug form
    7. Save the template. In the database (or API response), verify the stored expression contains `{{cf::abc123}}` (encoded _key form) not the slug
    8. Reload the template in the designer — the Expression textarea should show `{{cf::product_color}}` (decoded back to slug form)
  </how-to-verify>
  <verify>
    <automated>MISSING — manual verification only for this checkpoint</automated>
  </verify>
  <done>Toolbar button works, encode/decode round-trip confirmed by user</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Manual smoke test per Task 3 checkpoint covers TMPL-01 and TMPL-04.
Automated: `cd webapps/main && npx vitest run src/lib/print/` must pass green.
</verification>

<success_criteria>
- Toolbar button exists and calls `addField('template_string')`
- `initTemplate()` calls `decodeExpression` for all `template_expression` schema fields
- `saveTemplate()` calls `encodeExpression` on a fresh `designer.getTemplate()` copy before the API call
- `workingTemplate.value` is never mutated with encoded form
- i18n keys present in both `en.js` and `it.js`
- All vitest tests green
- Human checkpoint: toolbar button works, round-trip encode/decode verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-template-string-plugin/01-04-SUMMARY.md`
</output>
