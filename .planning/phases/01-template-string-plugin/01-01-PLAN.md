---
phase: 01-template-string-plugin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - webapps/main/vitest.config.js
  - webapps/main/src/lib/print/templateResolver.test.js
  - webapps/main/src/lib/print/plugins/linkedTemplateString.test.js
autonomous: true
requirements:
  - TMPL-02
  - TMPL-03

must_haves:
  truths:
    - "Running `npx vitest run src/lib/print/templateResolver.test.js` exits non-zero with failing tests (RED state)"
    - "Running `npx vitest run src/lib/print/plugins/linkedTemplateString.test.js` exits non-zero with failing tests (RED state)"
    - "vitest can locate and parse both test files without configuration errors"
  artifacts:
    - path: "webapps/main/vitest.config.js"
      provides: "vitest configuration pointing at src/"
      contains: "defineConfig"
    - path: "webapps/main/src/lib/print/templateResolver.test.js"
      provides: "Failing unit tests for slugify, encodeExpression, decodeExpression, resolveExpression"
      min_lines: 30
    - path: "webapps/main/src/lib/print/plugins/linkedTemplateString.test.js"
      provides: "Failing unit test for plugin defaultSchema.linkType"
      min_lines: 10
  key_links:
    - from: "webapps/main/vitest.config.js"
      to: "webapps/main/src/lib/print/"
      via: "include/root config"
      pattern: "src"
---

<objective>
Install vitest and create the test scaffold that all subsequent implementation plans depend on.
Both test files start in RED state — they import modules that do not exist yet.

Purpose: Nyquist compliance requires automated verification commands exist before implementation begins.
Output: `vitest.config.js`, `templateResolver.test.js`, `linkedTemplateString.test.js` — all failing (RED).
</objective>

<execution_context>
@/Users/luca/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-template-string-plugin/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and write vitest config</name>
  <files>webapps/main/vitest.config.js</files>
  <action>
    From `webapps/main/`, install vitest as a dev dependency:

    ```
    cd webapps/main && npm install -D vitest
    ```

    Create `webapps/main/vitest.config.js`:

    ```javascript
    import { defineConfig } from 'vitest/config';

    export default defineConfig({
      test: {
        environment: 'node',
        include: ['src/**/*.test.js'],
      },
    });
    ```

    Verify vitest resolves by running:
    ```
    cd webapps/main && npx vitest --version
    ```
  </action>
  <verify>
    <automated>cd /Users/luca/dev/progress/progress-platform/webapps/main && npx vitest --version</automated>
  </verify>
  <done>vitest version prints without error; `vitest.config.js` exists at `webapps/main/vitest.config.js`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test stubs for templateResolver and linkedTemplateString plugin</name>
  <files>
    webapps/main/src/lib/print/templateResolver.test.js,
    webapps/main/src/lib/print/plugins/linkedTemplateString.test.js
  </files>
  <behavior>
    templateResolver.test.js — these tests import non-existent modules and MUST fail in RED:
    - `slugify('Product Color')` returns `'product_color'`
    - `slugify('Qty (pcs)')` returns `'qty'`
    - `slugify('  Hello__World  ')` returns `'hello_world'`
    - `encodeExpression('{{cf::product_color}}', [{name:'Product Color', _key:'abc123'}])` returns `'{{cf::abc123}}'`
    - `encodeExpression('Product: {{product.code}}', [])` returns `'Product: {{product.code}}'` (preset passthrough)
    - `decodeExpression('{{cf::abc123}}', [{name:'Product Color', _key:'abc123'}])` returns `'{{cf::product_color}}'`
    - `decodeExpression('Product: {{product.code}}', [])` returns `'Product: {{product.code}}'` (preset passthrough)
    - `resolveExpression('Product: {{product.code}} - Qty: {{serial.qt}} pcs', ctx, [])` returns `'Product: ABC - Qty: 10 pcs'` where ctx mocks `getPresetValue`
    - `resolveExpression('{{cf::abc123}}', ctx, [{_key:'abc123'}])` returns the mocked custom field value
    - Missing variable (no match) resolves to empty string

    linkedTemplateString.test.js — MUST fail in RED:
    - `createLinkedTemplateString()` returns an object with `propPanel.defaultSchema.linkType === 'template_expression'`
    - `createLinkedTemplateString()` returns an object with `propPanel.defaultSchema.templateExpression === ''`
    - `createLinkedTemplateString([]).pdf` is defined (inherited from base text plugin)
  </behavior>
  <action>
    Write `webapps/main/src/lib/print/templateResolver.test.js` importing from `'./templateResolver.js'` (does not exist yet — tests WILL fail with import error).

    Use vitest syntax:
    ```javascript
    import { describe, it, expect, vi } from 'vitest';
    import { slugify, encodeExpression, decodeExpression, resolveExpression } from './templateResolver.js';
    ```

    Write `webapps/main/src/lib/print/plugins/linkedTemplateString.test.js` importing from `'./linkedTemplateString.js'` (does not exist yet).

    ```javascript
    import { describe, it, expect } from 'vitest';
    import { createLinkedTemplateString } from './linkedTemplateString.js';
    ```

    For `resolveExpression` tests, create a mock context:
    ```javascript
    const mockCtx = {
      getPresetValue: (key) => ({ 'product.code': 'ABC', 'serial.qt': '10' }[key]),
      getCustomFieldValue: (key) => ({ 'abc123': 'Red' }[key]),
    };
    ```

    Both files MUST include all behavior cases listed above. Tests will error on import — that is the correct RED state.

    Run to confirm RED:
    ```
    cd webapps/main && npx vitest run src/lib/print/templateResolver.test.js 2>&1 | tail -5
    cd webapps/main && npx vitest run src/lib/print/plugins/linkedTemplateString.test.js 2>&1 | tail -5
    ```
    Both should exit with non-zero status and show import/module-not-found errors.
  </action>
  <verify>
    <automated>cd /Users/luca/dev/progress/progress-platform/webapps/main && npx vitest run src/lib/print/templateResolver.test.js; [ $? -ne 0 ] && echo "RED — correct" || echo "UNEXPECTED PASS"</automated>
  </verify>
  <done>
    Both test files exist and vitest can parse them. Both fail with module-not-found or similar import errors (RED state). No syntax errors in the test files themselves.
  </done>
</task>

</tasks>

<verification>
```
cd /Users/luca/dev/progress/progress-platform/webapps/main && npx vitest --version
ls src/lib/print/templateResolver.test.js src/lib/print/plugins/linkedTemplateString.test.js
npx vitest run src/lib/print/ 2>&1 | tail -10
```
Both test files exist, vitest version prints, both test runs fail with import errors (RED).
</verification>

<success_criteria>
- `webapps/main/vitest.config.js` exists and vitest resolves without error
- `templateResolver.test.js` has all 10 test cases and fails with import error
- `linkedTemplateString.test.js` has all 3 test cases and fails with import error
- `npm install -D vitest` reflected in `package.json` devDependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-template-string-plugin/01-01-SUMMARY.md`
</output>
