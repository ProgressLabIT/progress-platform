---
phase: 03-print-service
task: post-plan credential refactor
total_tasks: 6
status: in_progress
last_updated: 2026-03-19T11:45:55.148Z
---

<current_state>
Two rounds of infrastructure changes are complete but uncommitted. UAT has not yet started — all 6 tests are still pending. The credential management approach has been redesigned: instead of a static JWT generated at setup, the print service now authenticates via /api/auth using a dedicated print_service user.
</current_state>

<completed_work>

- Phase 03 plans (01 + 02) executed and committed (9bf7d414)
- UAT infra pre-flight fixes:
  1. .gitlab-ci.yml — added build:print-service CI job
  2. backend/api/endpoints/print.py — verify_print_service_token on stream + result endpoints
  3. backend/api/utils/auth.py — _verify_token_base refactor + verify_print_service_token
  4. backend/print-service/config.py — secrets_dir added
  5. deploy/compose/print.yaml — Docker secret, progress network, default API URL
  6. deploy/scripts/db_init.py — PrintJob collection + original token generation (now replaced)
  7. deploy/single_node_setup.yaml — init-db jwt secret + output mount + print service setup block
- Credential management refactor (this session):
  8. backend/api/models/auth.py — added scope field to TokenData (was silently dropped from JWT)
  9. backend/api/utils/auth.py — verify_print_service_token now checks USER_SESSION context + print_service scope
  10. deploy/scripts/db_init.py — adds print_service user (scope: print_service), removed JWT generation block
  11. backend/print-service/config.py — replaced api_token with username + api_password
  12. backend/print-service/main.py — added authenticate() function, token obtained at startup, passed to report_result
  13. deploy/compose/print.yaml — secret is now print_service_pwd (→ api_password)
  14. deploy/single_node_setup.yaml — generates print_service_pwd separately, writes file + swarm secret, no more token file wait
</completed_work>

<remaining_work>

- Commit all 10 modified/new files
- Resume UAT — /gsd:verify-work 3 — all 6 tests pending:
  1. Cold Start Smoke Test
  2. TCP Sender Unit Tests
  3. POST /print-job Creates Pending Record
  4. GET /print-jobs/stream Delivers SSE Events
  5. POST /print-jobs/{job_id}/result Updates Job Status
  6. Print Service Dockerfile Builds
</remaining_work>

<decisions_made>

- Replaced static JWT with print_service user + login-based auth — eliminates duplicated token logic in db_init, token refresh handled automatically on reconnect
- Added scope: str | None = None to TokenData — scope was silently dropped from JWT before; now encoded so verify_print_service_token can check it without a DB lookup
- verify_print_service_token now checks context=USER_SESSION + "print_service" in scope.split() — replaces the context=api+consumer_type=3psw combination
- print_service_pwd generated separately from the main secrets loop — loop pipes to Docker without saving value; password needs to be both a file (for compose) and a swarm secret (for init-db)
</decisions_made>

<blockers>

- UAT requires a live environment — human must run the tests
</blockers>

<context>
The original static-JWT approach worked but required db_init.py to duplicate JWT signing logic and the setup to wait for a token file. The new approach is cleaner: print_service is just a user like cadmin, setup is idempotent, and the print service handles its own auth lifecycle. The token expires after 24h but reconnects re-authenticate automatically.
</context>

<next_action>
1. Commit the 10 modified files (git add specific files listed in uncommitted_files above)
2. Resume UAT: /gsd:verify-work 3 — test 1 (Cold Start Smoke Test) first
</next_action>
